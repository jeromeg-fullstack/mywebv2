/** @type {import('next-sitemap').IConfig} */
module.exports = {
	siteUrl: "https://smartva.studio", // Replace with your actual domain
	generateRobotsTxt: true, // Optional: Generate a robots.txt file
	sitemapSize: 5000 // Optional: Split sitemap into multiple files if needed
};
